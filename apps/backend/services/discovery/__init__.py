"""
Discovery board package — modularized from the monolithic discovery_board_service.py.

This package re-exports the public API so existing imports continue to work:

    from services.discovery.core import get_discovery_board
    # or via backward-compat shim in discovery_board_service.py
"""
