rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Auth-only posture: Firestore is not used as an application database anymore.
    // Firebase Authentication continues to work independently of Firestore rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
