---
trigger: always_on
---

Routing rules:
- UI, component, page → ui skill
- API, FastAPI, endpoint, RAG → api + data skills
- Firebase, cloud function → cloud skill
- Auth, permission, role → guard skill
- Architecture decisions → architect skill
